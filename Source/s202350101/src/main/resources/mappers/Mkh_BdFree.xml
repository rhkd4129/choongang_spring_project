<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- com.oracle.s202350101.뒤에 매핑하고 싶은 모델쓰면 됩니다 
	 mapper 여러개 쓰셔도 됩니다. -->
	 
	 
<mapper namespace="com.oracle.s202350101.BdFreeMapper">
	<!-- user_id에 해당하는 게시물 -->
	<select id="FreeListSelect" parameterType="UserInfo" resultType="BdFree">
		Select a.app_id, a.app_name, b.subject, b.user_id, 
			   b.create_date, b.bd_count, b.good_count
		From   bd_free b, app_info a
		Where  a.app_id = b.app_id
		And    user_id = #{user_id}
	</select>
	
	<!-- user_id에 해당하는 count -->
	<!-- <select id="userIdQnaListCount" resultType="int">
		select count(*) 
		from   bd_qna 
		where  user_id = #{user_id}
	</select> -->
	
	<!-- user_id에 해당하는 count -->
	
	<!-- <select id="userIdQnaListCount" resultType="int">
		select 
			(select count(*) from bd_qna      where user_id = 'tester1') as a,
			(select count(*) from bd_free     where user_id = 'tester1') as b,
			(select count(*) from prj_bd_rep  where user_id = 'tester1')as c,
			(select count(*) from prj_bd_data where user_id = 'tester1')as d
		from DUAL;
	</select> -->
	
	
</mapper>

